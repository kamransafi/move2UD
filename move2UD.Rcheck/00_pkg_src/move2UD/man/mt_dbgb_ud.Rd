% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbgb_ud.R
\name{mt_dbgb_ud}
\alias{mt_dbgb_ud}
\title{Dynamic Bivariate Gaussian Bridge — Utilisation Distribution}
\usage{
mt_dbgb_ud(
  object,
  raster = NULL,
  location_error,
  margin = 15,
  window_size = 31,
  ext = 0.5,
  dim_size = 100,
  time_step = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{One of:
\itemize{
\item A \code{move2} object (single or multi-track, projected CRS).
\item An \code{mt_dbgb_variance} object from \code{\link[=mt_dbgb_variance]{mt_dbgb_variance()}}.
\item A named list of \code{mt_dbgb_variance} objects (multi-track).
}}

\item{raster}{A \code{terra::SpatRaster} defining the output grid, a numeric
cell size in map units, or \code{NULL} to auto-compute.}

\item{location_error}{Numeric scalar or vector of location errors in
map units. Must be positive.}

\item{margin}{Integer (odd). Margin for variance estimation. Only used
when \code{object} is a \code{move2} object.}

\item{window_size}{Integer (odd). Window size for variance estimation.
Only used when \code{object} is a \code{move2} object.}

\item{ext}{Numeric. Extension factor for the bounding box. Default 0.5.}

\item{dim_size}{Integer. Cells along the longest dimension. Default 100.}

\item{time_step}{Numeric or \code{NULL}. Integration time step in minutes.}

\item{verbose}{Logical. Print progress messages for multi-track.}

\item{...}{Additional arguments passed to methods.}
}
\value{
For single-track: a \code{terra::SpatRaster} (values sum to 1.0).
For multi-track: a multi-layer \code{terra::SpatRaster} on a common grid,
one named layer per track, each summing to 1.0.
}
\description{
Compute a utilisation distribution using the dynamic bivariate Gaussian
bridge model, which decomposes movement variance into parallel and
orthogonal components relative to the direction of travel.
}
\details{
The dBGB UD uses an anisotropic Gaussian kernel at each time step,
with the kernel elongated along the direction of travel. This produces
narrower UDs along directed segments and wider UDs where movement is
more random, better capturing the actual space use of the animal.
}
\examples{
\donttest{
library(move2)
library(sf)

fishers <- mt_read(mt_example())
fishers <- fishers[!st_is_empty(fishers), ]
f1 <- fishers[mt_track_id(fishers) == "F1", ]
f1_proj <- st_transform(f1, mt_aeqd_crs(f1))

ud <- mt_dbgb_ud(f1_proj, location_error = 25,
                  margin = 15, window_size = 31)
terra::plot(ud)
}

}
\references{
Kranstauber, B., Safi, K., & Bartumeus, F. (2014). Bivariate Gaussian
bridges: directional factorization of diffusion in Brownian bridge
models. \emph{Movement Ecology}, 2(1), 5. \doi{10.1186/2051-3933-2-5}
}
\seealso{
\code{\link[=mt_dbgb_variance]{mt_dbgb_variance()}} to compute variance separately,
\code{\link[=mt_dbbmm_ud]{mt_dbbmm_ud()}} for the isotropic variant.
}
