% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbbmm_ud.R
\name{mt_dbbmm_ud}
\alias{mt_dbbmm_ud}
\title{Dynamic Brownian Bridge Movement Model — Utilisation Distribution}
\usage{
mt_dbbmm_ud(
  object,
  raster = NULL,
  location_error,
  margin = 11,
  window_size = 31,
  ext = 0.5,
  dim_size = 100,
  time_step = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{One of:
\itemize{
\item A \code{move2} object (single or multi-track, projected CRS).
Variance is estimated internally via \code{\link[=mt_dbbmm_variance]{mt_dbbmm_variance()}}.
\item An \code{mt_dbbmm_variance} object from \code{\link[=mt_dbbmm_variance]{mt_dbbmm_variance()}}.
\item A named list of \code{mt_dbbmm_variance} objects (multi-track).
}}

\item{raster}{A \code{terra::SpatRaster} defining the output grid, or a
numeric scalar giving the cell size in map units, or \code{NULL} to
auto-compute from \code{dim_size} and \code{ext}. For multi-track input with
\code{NULL}, a common grid is computed from the combined extent.}

\item{location_error}{Numeric scalar or vector of location errors in
map units. Must be positive.}

\item{margin}{Integer (odd). Margin for variance estimation window.
Only used when \code{object} is a \code{move2} object.}

\item{window_size}{Integer (odd). Window size for variance estimation.
Only used when \code{object} is a \code{move2} object.}

\item{ext}{Numeric. Extension factor for the bounding box when
auto-creating the raster. Increase if the C kernel reports that the
grid is not large enough. Default 0.5.}

\item{dim_size}{Integer. Number of cells along the longest dimension
of the auto-generated raster. Higher values give finer resolution
but slower computation. Default 100.}

\item{time_step}{Numeric or \code{NULL}. Time step for the Brownian bridge
integration, in minutes. Defaults to 1/15 of the minimum time lag.
Smaller values give more precise UDs but are slower.}

\item{verbose}{Logical. If \code{TRUE}, print a computational size estimate.}
}
\value{
For single-track input: a \code{terra::SpatRaster} where cell
values sum to 1.0, representing the utilisation distribution.

For multi-track input: a multi-layer \code{terra::SpatRaster} with one
named layer per track, all on a common grid. Each layer sums to 1.0.
}
\description{
Compute a utilisation distribution (UD) from a movement track using
the dynamic Brownian bridge movement model. Accepts a \code{move2} object
directly (estimates variance internally), a pre-computed variance
object, or a named list of variance objects for multi-track input.
}
\details{
The UD is computed by evaluating the Brownian bridge probability
density at regular time steps along each segment and accumulating
the density onto a raster grid. The computation is implemented in C
with OpenMP parallelisation of the inner grid loops for performance.

For multi-track input, a common raster grid is computed from the
combined spatial extent of all tracks, ensuring that UDs are
directly comparable.
}
\examples{
\donttest{
library(move2)
library(sf)

fishers <- mt_read(mt_example())
fishers <- fishers[!st_is_empty(fishers), ]
f1 <- fishers[mt_track_id(fishers) == "F1", ]
f1_proj <- st_transform(f1, mt_aeqd_crs(f1))

# One-step
ud <- mt_dbbmm_ud(f1_proj, location_error = 25,
                   window_size = 31, margin = 11)
terra::plot(ud)

# Two-step (re-use variance for different raster settings)
var_obj <- mt_dbbmm_variance(f1_proj, location_error = 25,
                              window_size = 31, margin = 11)
ud_fine <- mt_dbbmm_ud(var_obj, location_error = 25, dim_size = 500)
}

}
\references{
Kranstauber, B., Kays, R., LaPoint, S. D., Wikelski, M., & Safi, K.
(2012). A dynamic Brownian bridge movement model to estimate utilization
distributions for heterogeneous animal movement. \emph{Journal of Animal
Ecology}, 81(4), 738-746. \doi{10.1111/j.1365-2656.2012.01955.x}
}
\seealso{
\code{\link[=mt_dbbmm_variance]{mt_dbbmm_variance()}} to compute variance separately,
\code{\link[=mt_dbgb_ud]{mt_dbgb_ud()}} for the directional variant, \code{\link[=mt_motion_variance]{mt_motion_variance()}}
to extract variances.
}
