% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbgb_variance_dyn.R
\name{mt_dbgb_variance}
\alias{mt_dbgb_variance}
\title{Dynamic Bivariate Gaussian Bridge Variance Estimation}
\usage{
mt_dbgb_variance(
  object,
  location_error,
  margin,
  window_size,
  parallel = FALSE,
  cores = NULL
)
}
\arguments{
\item{object}{A \code{move2} object in a projected coordinate system (not
longitude/latitude). Use \code{sf::st_transform(x, move2::mt_aeqd_crs(x))}
to project. Both single-track and multi-track objects are accepted.}

\item{location_error}{Numeric scalar or vector of location errors in
map units. Must be positive.}

\item{margin}{Integer (must be odd). Minimum locations on each side of
a potential breakpoint.}

\item{window_size}{Integer (must be odd). Number of locations in the
sliding window.}

\item{parallel}{Logical. If \code{TRUE}, use parallel processing for the
sliding window. Defaults to \code{FALSE}.}

\item{cores}{Integer or \code{NULL}. Number of cores for parallel processing.}
}
\value{
For single-track input: an \code{mt_dbgb_variance} S3 object
containing:
\describe{
\item{\code{para_sd}}{Numeric vector of parallel standard deviations.}
\item{\code{orth_sd}}{Numeric vector of orthogonal standard deviations.}
\item{\code{n_estim}}{Number of windows each position was estimated in.}
\item{\code{seg_interest}}{Logical vector of fully-covered segments.}
\item{\code{margin}, \code{window_size}}{Parameters used.}
\item{\code{track_data}}{Coordinates, timestamps, CRS from the input.}
}

For multi-track input: a named list of \code{mt_dbgb_variance} objects.
Tracks with too few locations are skipped with a warning.
}
\description{
Estimate dynamic parallel and orthogonal movement variances using a
sliding window approach with BIC-based breakpoint detection. The
bivariate Gaussian bridge decomposes the movement variance into a
component parallel to the direction of travel and one orthogonal to
it, providing more detail about the movement process than the
isotropic dBBMM.
}
\details{
The method extends the dBBMM by decomposing the variance into a
parallel component (along the direction of travel between consecutive
locations) and an orthogonal component (perpendicular to it). This
allows distinguishing directed movement (high parallel, low orthogonal
variance) from random movement (similar variances in both directions).

A directionality index can be computed from the variances:
\code{I_d = (para - orth) / (para + orth)}, where values near 0 indicate
Brownian motion and positive values indicate directional movement.
}
\examples{
\donttest{
library(move2)
library(sf)

fishers <- mt_read(mt_example())
fishers <- fishers[!st_is_empty(fishers), ]
f1 <- fishers[mt_track_id(fishers) == "F1", ]
f1_proj <- st_transform(f1, mt_aeqd_crs(f1))

var_obj <- mt_dbgb_variance(f1_proj, location_error = 25,
                             margin = 15, window_size = 31)
var_obj

# Directionality index
mv <- mt_motion_variance(var_obj)
I_d <- (mv$para - mv$orth) / (mv$para + mv$orth)
plot(mt_time(f1_proj), I_d, type = "l",
     xlab = "Time", ylab = "Directionality index")
}

}
\references{
Kranstauber, B., Safi, K., & Bartumeus, F. (2014). Bivariate Gaussian
bridges: directional factorization of diffusion in Brownian bridge
models. \emph{Movement Ecology}, 2(1), 5. \doi{10.1186/2051-3933-2-5}
}
\seealso{
\code{\link[=mt_dbgb_ud]{mt_dbgb_ud()}} to compute the UD, \code{\link[=mt_motion_variance]{mt_motion_variance()}} to
extract variances as a data.frame, \code{\link[=mt_dbbmm_variance]{mt_dbbmm_variance()}} for the
isotropic variant.
}
